name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.8+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  
  # UI 相關
  device_preview: ^1.1.0
  cupertino_icons: ^1.0.8

  # AI 核心 (關鍵修改 1：務必改為 ^0.4.6 以支援大檔案上傳)
  google_generative_ai: ^0.4.6 

  # 錄音與播放
  record: ^6.0.0
  audioplayers: ^6.0.0
  
  # 檔案與儲存
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.2
  file_picker: ^8.0.0
  
  # 工具
  # (關鍵修改 2：改回 0.20.2 以配合您的 Flutter 環境)
  intl: ^0.20.2             
  uuid: ^4.3.3
  share_plus: ^10.0.0
  http: ^1.6.0

  # PDF 與列印
  pdf: ^3.10.0
  printing: ^5.11.0
  
  # YouTube
  youtube_explode_dart: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0